package CollegeBoard;

public class error {
    // ArrayList<int> intList = new ArrayList<int>();
    // compile error: we cannot use a primitive (int) as the type parameter in a generic ArrayList
}



/*
 * Merge sort uses the "divide and conquer" approach to sort an array. This will end up being more efficient than insertion sort in 
 * the case where we have long unordered array. However if we have a very small almost sorted array, then insertion sort will 
 * outperform merge sort.
*/